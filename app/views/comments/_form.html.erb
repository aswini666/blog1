<%= form_for([@post, @post.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :Comment %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.submit %>
  </div> 
<% end %>